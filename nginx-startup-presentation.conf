# Nginx config pentru Startup Presentation
# Pune acest fișier în: /etc/nginx/conf.d/startup-presentation.conf

server {
    listen 80;
    listen [::]:80;
    server_name nextti.ro www.nextti.ro;  # Schimbă cu domeniul tău

    # Redirect HTTP -> HTTPS (decomentează după ce ai SSL)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Configurare HTTPS (decomentează după ce ai generat certificatul SSL)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name nextti.ro www.nextti.ro;
#
#     # Certificat SSL Let's Encrypt
#     ssl_certificate /etc/letsencrypt/live/nextti.ro/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/nextti.ro/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
